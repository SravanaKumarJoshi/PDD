<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.biopolymer.screening">

    <!-- Internet access — required for all API communication -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!--
      ACCESS_NETWORK_STATE is used by NetworkMonitor to detect connectivity
      changes before attempting API calls, enabling offline-first UX.
    -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".BiopolymerApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.BioPolymerScreening"
        android:networkSecurityConfig="@xml/network_security_config"
        tools:replace="android:theme">

        <!--
          networkSecurityConfig points to src/<flavor>/res/xml/network_security_config.xml.
          Each build flavor provides its own copy:
            dev        → allows HTTP to 10.0.2.2 and local RFC-1918 addresses only
            staging    → HTTPS only, system CA trust
            production → HTTPS only, system CA trust, certificate pinning

          The android:usesCleartextTraffic attribute is intentionally omitted here.
          Cleartext policy is fully controlled per-flavor via the XML config above,
          which is the recommended approach for Android 9+ (API 28+).
        -->

        <activity
            android:name=".SplashActivity"
            android:exported="true"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.BioPolymerScreening">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".LoginActivity"
            android:screenOrientation="portrait"
            android:windowSoftInputMode="adjustResize" />

        <activity
            android:name=".SignUpActivity"
            android:screenOrientation="portrait"
            android:windowSoftInputMode="adjustResize" />

        <activity
            android:name=".MainActivity"
            android:exported="false"
            android:screenOrientation="portrait"
            android:windowSoftInputMode="adjustResize" />

    </application>

</manifest>
